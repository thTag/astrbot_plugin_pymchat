{
    "api_key": {
        "description": "PymChat API 密钥",
        "type": "string",
        "hint": "登录 https://chat.qplm.xyz 后获取，有效期30天，过期后需重新登录获取",
        "obvious_hint": true
    },
    "username": {
        "description": "用户名（用于自动获取API密钥，与api_key二选一）",
        "type": "string",
        "hint": "如果填写了用户名密码，插件将在首次使用时自动登录获取API密钥"
    },
    "password": {
        "description": "密码（与username配对使用）",
        "type": "string"
    },
    "debug_mode": {
        "description": "调试模式，开启后输出详细日志",
        "type": "boolean",
        "default": false
    }
}